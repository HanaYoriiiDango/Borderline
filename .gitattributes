# Automatic encoding handling for C++ projects
*.cpp text working-tree-encoding=UTF-8
*.h text working-tree-encoding=UTF-8
*.hpp text working-tree-encoding=UTF-8
*.json text working-tree-encoding=UTF-8
*.txt text
*.md text
*.bat text

# Never commit BOM
*.cpp -bom
*.h -bom
*.hpp -bom
*.json -bom

# Line endings (Windows style)
* text=auto eol=crlf

# Except for shell scripts (if any)
*.sh text eol=lf